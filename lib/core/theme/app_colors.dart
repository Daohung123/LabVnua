import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF0047A8);
  static const Color primaryLight = Color(0xFFE8EFFC);
  static const Color primaryDark = Color(0xFF003A85);

  // Secondary colors (instructor role)
  static const Color secondary = Color(0xFF00897B);
  static const Color secondaryLight = Color(0xFFE0F5F3);

  // Background
  static const Color bgPage = Color(0xFFF4F7FE);
  static const Color bgCard = Colors.white;

  // Text
  static const Color textTitle = Color(0xFF0D1B3E);
  static const Color textBody = Color(0xFF5A6A85);
  static const Color textHint = Color(0xFF8E9BB8);
  static const Color textSecondary = Color(0xFF7A89A8);

  // Divider & Border
  static const Color divider = Color(0xFFE2E8F4);
  static const Color borderDefault = Color(0xFFE2E8F4);
  static const Color borderLight = Color(0xFFF0F4FC);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFFF5252);
  static const Color warning = Color(0xFFFFC107);
  static const Color warningBg = Color(0xFFFFF8E7);
  static const Color warningBorder = Color(0xFFFFDC82);
  static const Color warningIcon = Color(0xFFE8A500);
  static const Color warningText = Color(0xFF7A5C00);

  // Gradients
  static const List<Color> primaryGradient = [
    Color(0xFF0047A8),
    Color(0xFF2979FF),
  ];
}
