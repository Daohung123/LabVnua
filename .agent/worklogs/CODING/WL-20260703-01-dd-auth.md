# WL-20260703-01 - DD_AUTH MVP Coding

- `STATUS:` DONE
- `TYPE:` CODING
- `SCOPE:` AUTH
- `PATTERN:` dd-backed-flutter-auth-ui
- `TAGS:` auth, login, session, avatar-menu, flutter
- `REF:` `docs/checklists/coding/CODING-AUTH-20260703-dd-auth.md`

## CURRENT

- Implemented source-backed MVP portions of `docs/DD/DD_AUTH/` without inventing VNied OAuth2, secure storage, or role routing contracts.

## CHANGED

- Auth entry now sends unauthenticated users to `LoginScreen` instead of manual role selection.
- Login UI keeps current VNUA email/password flow and shows VNied as a disabled OAuth2 stub.
- Home header now includes an avatar dropdown with settings, disabled change-password, and logout.
- Logout now stops chat realtime/notification services and deletes the local session before returning to login.
- SQLite session presence check now reads the SQL count value.
- Added focused widget tests for login validation, VNied disabled state, and avatar logout callback.

## NOTE

- Successful login still routes to the existing `HomeScreen` because role mapping and teacher route contracts are unresolved.
- Full `flutter analyze` remains blocked by repo-wide pre-existing lint debt outside AUTH scope.
- Scoped analyzer over changed AUTH files and `flutter test` passed.

## TASK SPLIT

- `[x]` Read `AGENTS.md` and classify task as `CODING`.
- `[x]` Load targeted AUTH DD and relevant auth/home/session code.
- `[x]` Implement DD_AUTH MVP source-backed behavior.
- `[x]` Add focused widget tests.
- `[x]` Run verification and record full-analyzer limitation.
- `[x]` Update checklist and skill counter.

## NEXT

1. Resolve VNied OAuth2 endpoint/client/redirect/scope contract before enabling the VNied button.
2. Resolve role source and teacher route before changing post-login routing.
3. Decide secure-storage policy before changing session persistence.
